# Punto 2 — Rectángulo (MVC, interactivo, Maven)
- Clases (mín. 4): `Figura` (abstracta), `Punto`, `Rectangulo`, `ControladorFiguras`, `VistaConsola` + `Principal`.
- Requisitos: crear rectángulo con vértices opuestos, área, escalar manteniendo p1, contiene(punto), mover H/V (un sentido a la vez).
- POO: herencia (`Figura` → `Rectangulo`), polimorfismo (`area`, `mover*`, `contiene`), sobrecarga (`Rectangulo` y `escalar`).
- MVC y **vista** por consola con interacción de usuario.
- Tests unitarios e integración.
